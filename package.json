{
  "name": "install-yb-action",
  "version": "0.1.0",
  "description": "Run the YourBase build tool",
  "main": "dist/index.js",
  "scripts": {
    "build": "node scripts/build.js",
    "compile": "tsc --build tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "YourBase",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/yourbase/install-yb-action.git"
  },
  "dependencies": {
    "@actions/core": "^1.6.0",
    "@actions/github": "^5.0.0",
    "esbuild": "^0.16.11"
  },
  "devDependencies": {
    "@types/node": "^12.20.4",
    "typescript": "^4.2.2"
  }
}
